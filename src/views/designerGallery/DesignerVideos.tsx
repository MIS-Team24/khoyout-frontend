import NavigatablePageWrapper from "@/components/custom/NavigatablePageWrapper";
import { DesignerGalleryVideos } from "@/components/views/designerGallery";

export default function DesignerVideos() {
  return (
    <main>
      <NavigatablePageWrapper>
        <DesignerGalleryVideos />
      </NavigatablePageWrapper>
    </main>
  );
}
