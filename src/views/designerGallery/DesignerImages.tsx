import NavigatablePageWrapper from "@/components/custom/NavigatablePageWrapper";
import { DesignerGalleryImages } from "@/components/views/designerGallery";

export default function DesignerImages() {
  return (
    <main>
      <NavigatablePageWrapper>
        <DesignerGalleryImages />
      </NavigatablePageWrapper>
    </main>
  );
}
